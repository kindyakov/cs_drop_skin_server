# 🛠️ Сервисы - Справочник

## 📖 Описание

Справочник сервисов проекта с описанием функций бизнес-логики в папке `src/services/`.

Сервисы инкапсулируют бизнес-логику и работу с базой данных, изолируя её от контроллеров и роутов.

---

## 🎮 Case Service (`case.service.ts`)

Сервис для работы с кейсами (только операции чтения).

### **Функции чтения**

#### **`getAllActiveCases()`**

- **Описание:** Получение всех активных кейсов
- **Тип:** `() => Promise<ICase[]>`
- **Логика:**
  - Фильтрует по `isActive: true`
  - Сортирует по `createdAt` (новые первыми)
- **Prisma запрос:**
  ```typescript
  await prisma.case.findMany({
    where: { isActive: true },
    orderBy: { createdAt: 'desc' },
  });
  ```
- **Использование:** Список кейсов для главной страницы

#### **`getCaseBySlug(slug)`**

- **Описание:** Получение кейса по slug с предметами
- **Тип:** `(slug: string) => Promise<ICaseWithItems>`
- **Логика:**
  - Находит кейс по slug
  - Загружает связанные предметы
  - Сортирует по вероятности выпадения
  - Конвертирует Decimal в number
- **Prisma запрос:**
  ```typescript
  await prisma.case.findUnique({
    where: { slug },
    include: {
      items: {
        include: { item: true },
        orderBy: { chancePercent: 'desc' },
      },
    },
  });
  ```
- **Обработка ошибок:** `NotFoundError('Кейс не найден')`
- **Использование:** Страница детальной информации о кейсе

#### **`getCaseById(id)`**

- **Описание:** Получение кейса по ID
- **Тип:** `(id: string) => Promise<ICase>`
- **Логика:** Поиск по ID без предметов
- **Prisma запрос:**
  ```typescript
  await prisma.case.findUnique({ where: { id } });
  ```
- **Обработка ошибок:** `NotFoundError('Кейс не найден')`
- **Использование:** Внутренние операции, проверка существования

### **⚠️ Important**

#### **Только операции чтения:**

- ❌ **Нет create/update/delete** (это для админ сервиса)
- ✅ **Только query операции** для пользователей
- 🚫 **Нет кэширования** (добавим позже)
- 🚫 **Нет сложной бизнес-логики**

#### **Типизация:**

- **ICase** - базовый кейс (без предметов)
- **ICaseWithItems** - кейс с деталями предметов
- **NotFoundError** - из utils для обработки 404

#### **Работа с ценами:**

- **price** - в копейках (Int в Prisma)
- **chancePercent** - Decimal → number конвертация
- **Консистентное форматирование** во всём приложении

---

## 🏗️ Архитектура сервисов

### **Структура layer:**

```
Controller → Service → Prisma → Database
     ↓           ↓         ↓          ↓
  Request  →  Business  →  Query  →  Data
```

### **Преимущества сервисов:**

- **Изоляция бизнес-логики** от HTTP слоя
- **Переиспользование** в разных контроллерах
- **Простое тестирование** бизнес-логики
- **Чистые контроллеры** без запросов к БД
- **Easier рефакторинг** запросов

---

## 📦 Импорт и использование

```typescript
// Прямой импорт конкретного сервиса
import { getAllActiveCases, getCaseBySlug } from '../services/case.service.js';

// В контроллере
export const getCases = async (_req: Request, res: Response) => {
  try {
    const cases = await getAllActiveCases();
    successResponse(res, cases);
  } catch (error) {
    next(error);
  }
};
```

---

## 👤 User Service (`user.service.ts`)

Сервис для работы с профилями пользователей и их инвентарем.

### **Функции профиля пользователя**

#### **`getProfileById(userId, requestingUserId?)`**

- **Описание:** Получение профиля пользователя по ID
- **Тип:** `(userId: string, requestingUserId?: string) => Promise<IUserPublicProfile>`
- **Логика:**
  - Загружает пользователя с favoriteCase и bestDrop через include
  - Загружает первые 21 предмет инвентаря
  - Считает общее количество предметов (`totalItems`)
  - Вычисляет флаг `hasMore` (есть ли ещё предметы)
  - Считает количество открытий favorite case (`openingsCount`)
  - **Публичный режим** (без requestingUserId или другой пользователь):
    - Возвращает `IUserPublicProfile`
    - Скрывает balance, tradeUrl, isBlocked
- **Обработка ошибок:** Возвращает `null` если пользователь не найден
- **Использование:**

```typescript
// Публичный профиль (чужой)
const profile = await getProfileById('user123');

// Свой профиль (расширенный)
const myProfile = await getProfileById('user123', 'user123');
```

#### **`getUserInventory(userId, limit?, offset?)`**

- **Описание:** Получение инвентаря пользователя с пагинацией
- **Тип:** `(userId: string, limit?: number, offset?: number) => Promise<IUserItem[]>`
- **Логика:**
  - Фильтрует по `userId` и `status: 'OWNED'`
  - Загружает связанные данные предметов через include
  - Сортирует по `acquiredAt` (новые первыми)
  - Поддержка пагинации через limit/offset
- **Параметры:**
  - `limit` - количество предметов (по умолчанию 21)
  - `offset` - смещение для пагинации (по умолчанию 0)
- **Использование:**

```typescript
// Первая страница (первые 21 предмет)
const items = await getUserInventory('user123', 21, 0);

// Вторая страница (следующие 21 предмет)
const moreItems = await getUserInventory('user123', 21, 21);
```

#### **`getUserOpenings(userId, limit?)`**

- **Описание:** Получение истории открытий кейсов пользователя
- **Тип:** `(userId: string, limit?: number) => Promise<CaseOpening[]>`
- **Логика:**
  - Фильтрует по `userId`
  - Загружает связанные данные кейса и предмета
  - Сортирует по `openedAt` (новые первыми)
  - Ограничение через параметр limit
- **Параметры:**
  - `limit` - количество записей (по умолчанию 50)

#### **`updateUserTradeUrl(userId, tradeUrl)`**

- **Описание:** Обновление Steam trade URL пользователя
- **Тип:** `(userId: string, tradeUrl: string) => Promise<void>`
- **Логика:**
  - Обновляет поле tradeUrl в БД
  - Валидация формата происходит на уровне middleware
- **Использование:** Endpoint для пользователя чтобы установить/обновить trade URL

#### **`updateUserStats(userId, caseId, itemId, itemPrice)`**

- **Описание:** Автоматическое обновление статистики пользователя после открытия кейса
- **Тип:** `(userId: string, caseId: string, itemId: string, itemPrice: number) => Promise<void>`
- **Логика:**
  - **Favorite Case:** Вычисляет самый часто открываемый кейс через groupBy
  - **Best Drop:** Сравнивает цену нового предмета с текущим best drop
  - Обновляет favoriteCaseId и bestDropItemId одним запросом
  - НЕ бросает ошибки наружу (обёрнуто в try-catch)
- **Когда вызывается:** Автоматически после успешного открытия кейса
- **Алгоритм Favorite Case:**

```typescript
// Группировка по caseId, сортировка по количеству, взять топ-1
const caseStats = await prisma.caseOpening.groupBy({
  by: ['caseId'],
  where: { userId },
  _count: { id: true },
  orderBy: { _count: { id: 'desc' } },
  take: 1,
});
```

- **Алгоритм Best Drop:**
  - Если bestDropItemId === null → установить текущий предмет
  - Если bestDropItemId существует → сравнить цены
  - Если новый предмет дороже → обновить
- **Безопасность:** Ошибки не прерывают основной функционал открытия кейса

### **⚠️ Important**

#### **Пагинация инвентаря:**

- Первоначальная загрузка: 21 предмет в `getProfileById`
- Дополнительная загрузка: через `getUserInventory` с offset
- Флаг `hasMore` в профиле показывает, есть ли ещё предметы

#### **Favorite Case & Best Drop:**

- Загружаются автоматически через Prisma relations
- Если не установлены (null) - возвращается null
- Обновляются автоматически при открытии кейсов (см. caseOpening.service.ts)

---

## 🛒 Market CSGO Service (`marketCs.service.ts`)

Сервис для интеграции с market.csgo.com API и синхронизации скинов.

### **Функции интеграции**

#### **`fetchItemsFromMarket()`**

- **Описание:** Получение списка скинов из API market.csgo.com
- **Тип:** `() => Promise<MarketItem[]>`
- **Логика:**
  - HTTP GET запрос к `/api/v2/prices/USD.json`
  - Передача API ключа через query параметры
  - Таймаут запроса: 30 секунд
  - Парсинг ответа и конвертация в MarketItem[]
- **API запрос:**
  ```typescript
  await axios.get('https://market.csgo.com/api/v2/prices/USD.json', {
    params: { key: config.marketCs.apiKey },
    timeout: 30000,
  });
  ```
- **Обработка ошибок:** Логирование ошибки через logger
- **Использование:** Внутренняя функция для получения данных

#### **`syncItems()`**

- **Описание:** Синхронизация скинов с базой данных
- **Тип:** `() => Promise<{ added: number; updated: number }>`
- **Логика:**
  - Получение данных через `fetchItemsFromMarket()`
  - Итерация по каждому скину
  - Поиск существующего `item` по `marketHashName`
  - Конвертация цен: USD центы → RUB копейки
  - Создание новых или обновление существующих записей
- **Правила обработки:**
  - **Существующий:** обновление только цены
  - **Новый:** создание с rarity 'CONSUMER'
- **Логирование:**
  - `info` в начале и конце синхронизации
  - `error` при сбоях с детальной информацией
- **Использование:** Автоматическая синхронизация по расписанию

### **⚠️ Important**

#### **Только операции чтения и синхронизации:**

- ❌ **Нет бизнес-логики** (ценообразование, analytics)
- ✅ **Только чтение из API** и запись в БД
- 🚫 **Нет обработки ошибок API** (базовая обработка)
- 🚫 **Нет сложной конвертации валют**

#### **Типизация:**

- **MarketItem interface** - для данных из API
- **Анонимный тип** для результата синхронизации
- **Config integration** - API ключ из конфигурации

#### **Интеграции:**

- **Axios** для HTTP запросов к внешнему API
- **Prisma** для операций с базой данных
- **Logger** для логирования операций
- **Config** для получения API ключа

---

## 💳 Payment Service (`payment.service.ts`)

Сервис для работы с платежами через YooKassa API.

### **Функции платежей**

#### **`createPayment(request)`**

- **Описание:** Создание нового платежа в YooKassa
- **Тип:** `(input: ICreatePaymentRequest) => Promise<{ confirmationUrl: string; transactionId: string }>`
- **Логика:**
  - Валидация минимальной суммы (10 рублей)
  - Создание записи транзакции в БД со статусом PENDING
  - Генерация idempotence key через uuidv4()
  - Отправка запроса к YooKassa API
  - Сохранение payment_id в транзакции
  - Возврат URL для редиректа на оплату
- **YooKassa запрос:**
  ```typescript
  await axios.post<IYooKassaCreatePaymentResponse>(
    'https://api.yookassa.ru/v3/payments',
    {
      amount: { value: (input.amount / 100).toFixed(2), currency: 'RUB' },
      confirmation: { type: 'redirect', return_url: returnUrl },
      capture: true,
      metadata: { userId, transactionId },
      description: 'Пополнение баланса',
    },
    {
      auth: { username: config.yookassa.shopId, password: config.yookassa.secretKey },
      headers: { 'Idempotence-Key': idempotenceKey },
    }
  );
  ```
- **Валидация:** Минимальная сумма 1000 копеек (10 рублей)
- **Использование:** Создание платежа для пополнения баланса

#### **`processWebhook(webhook)`**

- **Описание:** Обработка webhook уведомлений от YooKassa
- **Тип:** `(webhook: IYooKassaWebhook) => Promise<void>`
- **Логика:**
  - Извлечение данных из webhook (paymentId, status, metadata)
  - Поиск транзакции по paymentId и transactionId
  - Проверка статуса (если не PENDING - пропускаем)
  - При success: зачисление средств на баланс пользователя
  - При canceled: обновление статуса транзакции на FAILED
  - Все операции в транзакции БД для консистентности
- **Обрабатываемые статусы:**
  - `succeeded` → списание с баланса + status COMPLETED
  - `canceled` → status FAILED
- **Использование:** Endpoint для приема webhook от YooKassa

### **⚠️ Important**

#### **Только операции пополнения:**

- ❌ **Нет вывода средств** (withdrawal)
- ✅ **Только DEPOSITE** транзакции
- 🚫 **Нет проверки подписи webhook** (упрощено)
- 🚫 **Нет сложной бизнес-логики**

#### **Безопасность:**

- **Idempotence ключ** для предотвращения дублирования
- **Transaction metadata** для связки платежей с пользователями
- **Database transactions** для атомарности операций
- **Validation** минимальной суммы пополнения

#### **Интеграции:**

- **YooKassa API v3** - создание платежей
- **Prisma** - операции с базой данных
- **UUID** - генерация idempotence ключей
- **Logger** - логирование всех операций

#### **Конвертация данных:**

- **Суммы:** Копейки ↔ Рубли для API
- **Статусы:** YooKassa ↔ Internal статусы
- **ID:** Internal transaction ID ↔ YooKassa payment ID

---

## 🔐 Admin Case Service (`admin/adminCase.service.ts`)

Сервис для CRUD операций с кейсами (только для администраторов).

### **Функции управления кейсами**

#### **`createCase(input)`**

- **Описание:** Создание нового кейса
- **Тип:** `(input: ICreateCaseInput) => Promise<ICase>`
- **Логика:**
  - Генерирует slug из name через `slugify()`
  - Проверяет уникальность slug
  - Создаёт кейс с isActive = true по умолчанию
- **Валидация:** ValidationError при дублировании slug
- **Логирование:** Info при успехе, error при ошибке

#### **`updateCase(id, input)`**

- **Описание:** Обновление существующего кейса
- **Тип:** `(id: string, input: IUpdateCaseInput) => Promise<ICase>`
- **Логика:**
  - Проверяет существование кейса
  - При изменении name - обновляет slug и проверяет уникальность
  - Обновляет только переданные поля
- **Обработка ошибок:**
  - `NotFoundError` если кейс не найден
  - `ValidationError` при дублировании slug

#### **`deleteCase(id)`**

- **Описание:** Soft delete кейса
- **Тип:** `(id: string) => Promise<void>`
- **Логика:**
  - Проверяет существование кейса
  - Устанавливает isActive = false
  - НЕ удаляет данные из БД
- **Обработка ошибок:** `NotFoundError` если кейс не найден
- **Примечание:** Кейс остаётся в БД, но скрыт от пользователей

#### **`addItemsToCase(caseId, input)`**

- **Описание:** Добавление предметов в кейс
- **Тип:** `(caseId: string, input: IAddItemsToCaseInput) => Promise<ICaseWithItems>`
- **Логика:**
  - Проверяет существование кейса
  - Валидирует сумму шансов = 100% (допуск 0.01)
  - Проверяет существование всех предметов
  - Добавляет предметы в транзакции
  - Возвращает кейс с предметами
- **Валидация:**
  - Сумма chancePercent должна быть 100%
  - Все itemId должны существовать в БД
- **Обработка ошибок:**
  - `NotFoundError` если кейс или предметы не найдены
  - `ValidationError` если сумма шансов != 100%

### **⚠️ Important**

#### **Безопасность:**

- ✅ **Только для администраторов** - требуется requireAdmin middleware
- ✅ **Детальное логирование** всех операций для аудита
- ✅ **Soft delete** - данные не удаляются из БД
- ✅ **Транзакции** для атомарности операций

#### **Валидация:**

- **Уникальность slug** при создании и обновлении
- **Сумма шансов = 100%** при добавлении предметов
- **Существование предметов** перед добавлением

#### **Интеграция:**

- **slugify()** для генерации URL-friendly slug
- **Prisma $transaction** для атомарных операций
- **Logger** для логирования всех действий

---

## 📁 Admin Category Service (`admin/adminCategory.service.ts`)

Сервис для управления категориями кейсов (только для администраторов).

### **Функции управления категориями**

#### **`getAllCategories()`**

- **Описание:** Получение всех категорий с количеством кейсов
- **Тип:** `() => Promise<ICategoryWithCount[]>`
- **Логика:**
  - Загружает все категории
  - Включает счетчик кейсов (\_count)
  - Сортирует по полю order (ASC)

#### **`getCategoryById(id)`**

- **Описание:** Получение категории с кейсами
- **Тип:** `(id: string) => Promise<ICategoryWithCases>`
- **Логика:**
  - Находит категорию по ID
  - Загружает связанные кейсы
  - Возвращает детальную информацию
- **Обработка ошибок:** `NotFoundError` если не найдена

#### **`createCategory(input)`**

- **Описание:** Создание новой категории
- **Тип:** `(input: ICreateCategoryInput) => Promise<ICategory>`
- **Логика:**
  - Генерирует slug через slugify()
  - Проверяет уникальность slug
  - Создаёт категорию
- **Валидация:** `ValidationError` при дублировании

#### **`updateCategory(id, input)`**

- **Описание:** Обновление категории
- **Тип:** `(id: string, input: IUpdateCategoryInput) => Promise<ICategory>`
- **Логика:**
  - Проверяет существование
  - Обновляет slug при изменении name
  - Проверяет уникальность нового slug

#### **`deleteCategory(id)`**

- **Описание:** Soft delete категории
- **Тип:** `(id: string) => Promise<void>`
- **Логика:**
  - Убирает категорию у всех кейсов (categoryId = null)
  - Устанавливает isActive = false
  - Выполняется в транзакции

#### **`assignCasesToCategory(categoryId, input)`**

- **Описание:** Назначение кейсов категории
- **Тип:** `(categoryId: string, input: IAssignCasesToCategoryInput) => Promise<ICategoryWithCases>`
- **Логика:**
  - Проверяет существование категории
  - Проверяет существование всех кейсов
  - Назначает categoryId кейсам
  - Возвращает категорию с обновлённым списком кейсов

### **⚠️ Important**

#### **Связи:**

- Категория → Много кейсов (one-to-many)
- Кейс может быть без категории (categoryId = NULL)
- При удалении категории кейсы остаются (SetNull)

#### **Порядок сортировки:**

- Поле `order` для управления позицией категории
- Меньшее значение = выше в списке

---

## 🚀 Future Services (Планируемые)

### **Case Opening Service** (`caseOpening.service.ts`)

- Алгоритм выпадения предметов
- Обработка открытий кейсов
- Live feed события

### **Payment Service** (`payment.service.ts`)

- Интеграция с платёжными системами
- Пополнение баланса
- Обработка транзакций

### **Admin Service** (`admin.service.ts`)

- CRUD операций для кейсов
- Управление предметами
- Аналитика и статистика

---

## 🔗 Интеграции

### **Dependencies:**

- **Prisma Client** - работа с базой данных
- **Types** - TypeScript интерфейсы
- **Utils** - ошибки и общие функции

### **Error Handling:**

- **NotFoundError** - для отсутствующих ресурсов
- **Repository pattern** - инкапсуляция запросов
- **Consistent responses** - Unified error handling

---

## 👥 Admin User Service (`admin/adminUser.service.ts`)

Сервис для управления пользователями (только для администраторов).

### **Функции управления пользователями**

#### **`getAllUsers(filters)`**

- **Описание:** Получение списка пользователей с фильтрами
- **Тип:** `(filters: IGetUsersFilters) => Promise<IUser[]>`
- **Логика:**
  - Фильтрация по role (USER/ADMIN)
  - Поиск по username (case-insensitive)
  - Пагинация через limit и offset
  - Сортировка по createdAt (desc)
- **Параметры:**
  - `role` - фильтр по роли
  - `search` - поиск по username
  - `limit` - количество записей (по умолчанию 50)
  - `offset` - смещение для пагинации

#### **`toggleUserBlock(userId)`**

- **Описание:** Блокировка/разблокировка пользователя
- **Тип:** `(userId: string) => Promise<IUser>`
- **Логика:**
  - Проверяет существование пользователя
  - Запрещает блокировку администраторов
  - Переключает поле isBlocked
  - Логирует действие
- **Валидация:**
  - `NotFoundError` если пользователь не найден
  - `ValidationError` при попытке заблокировать админа

#### **`updateUserBalance(userId, input)`**

- **Описание:** Обновление баланса пользователя админом
- **Тип:** `(userId: string, input: IUpdateUserBalanceInput) => Promise<IUser>`
- **Логика:**
  - Проверяет существование пользователя
  - Рассчитывает новый баланс
  - Запрещает отрицательный баланс
  - Создаёт транзакцию для истории
  - Обновляет баланс в БД
  - Всё выполняется в Prisma транзакции
- **Валидация:**
  - `NotFoundError` если пользователь не найден
  - `ValidationError` если новый баланс отрицательный
- **Примечание:** amount может быть как положительным (пополнение), так и отрицательным (списание)

### **⚠️ Important**

#### **Безопасность:**

- ✅ **Только для администраторов** - требуется requireAdmin middleware
- ✅ **Защита админов** - нельзя заблокировать администратора
- ✅ **Транзакции** для атомарности операций с балансом
- ✅ **Детальное логирование** всех админских действий

#### **Блокировка пользователей:**

- Заблокированные пользователи не могут открывать кейсы
- Заблокированные пользователи не могут пополнять баланс
- Проверка через `checkUserBlocked` middleware

#### **История операций:**

- При изменении баланса создаётся транзакция типа DEPOSIT/WITHDRAWAL
- Статус автоматически COMPLETED
- Можно отследить все admin adjustments

---

## 📊 Admin Stats Service (`admin/adminStats.service.ts`)

Сервис для получения статистики и аналитики (только для администраторов).

### **Функции статистики**

#### **`getDashboardStats()`**

- **Описание:** Получение статистики дашборда админ-панели
- **Тип:** `() => Promise<IAdminDashboardStats>`
- **Логика:**
  - Вычисляет временные метки (today, week, month)
  - Параллельные запросы через Promise.all для оптимизации
  - Агрегирует данные по пользователям, кейсам, открытиям, доходам
- **Метрики:**
  - **Users:** total, newToday, newThisWeek
  - **Revenue:** total, today, thisWeek, thisMonth (в копейках)
  - **Openings:** total, today, thisWeek
  - **Cases:** total, active
- **Оптимизация:** Все запросы выполняются параллельно

#### **`getPopularCases(limit)`**

- **Описание:** Получение самых популярных кейсов
- **Тип:** `(limit: number = 10) => Promise<IPopularCase[]>`
- **Логика:**
  - Группировка открытий по caseId
  - Подсчёт количества открытий
  - Расчёт дохода (price \* openingsCount)
  - Сортировка по количеству открытий (desc)
  - Загрузка деталей кейсов
- **Параметры:**
  - `limit` - количество кейсов (по умолчанию 10)
- **Возвращает:** Массив с id, name, imageUrl, openingsCount, revenue

#### **`getRecentTransactions(limit)`**

- **Описание:** Получение последних транзакций
- **Тип:** `(limit: number = 20) => Promise<IRecentTransaction[]>`
- **Логика:**
  - Загружает транзакции с пользователями
  - Сортирует по createdAt (desc)
  - Форматирует в IRecentTransaction
- **Параметры:**
  - `limit` - количество транзакций (по умолчанию 20)
- **Возвращает:** Массив транзакций с деталями пользователя

### **⚠️ Important**

#### **Производительность:**

- ✅ **Параллельные запросы** - Promise.all для dashboard stats
- ✅ **Оптимизированные агрегации** - Prisma aggregate и groupBy
- ✅ **Selective includes** - только нужные поля

#### **Метрики дохода:**

- Считается только DEPOSIT транзакции
- Только со статусом COMPLETED
- Все суммы в копейках

#### **Временные периоды:**

- **Today:** С начала текущего дня (00:00)
- **This Week:** Последние 7 дней
- **This Month:** С начала текущего месяца

---

## 🎯 Case Opening Service (`caseOpening.service.ts`)

Сервис для обработки открытий кейсов и генерации live-feed событий.

### **Функции открытия кейсов**

#### **`openCase(userId, caseId)`**

- **Описание:** Открытие кейса пользователем
- **Тип:** `(userId: string, caseId: string) => Promise<ICaseOpeningResult>`
- **Логика:**
  - Проверяет существование пользователя и кейса
  - Проверяет достаточность баланса
  - Выбирает предмет по алгоритму weighted random
  - В транзакции:
    - Списывает баланс
    - Добавляет предмет в инвентарь
    - Создаёт запись CaseOpening
  - **Эмитит событие в WebSocket** - live-feed получает обновление
  - **Обновляет статистику пользователя** - favorite case и best drop
  - Возвращает результат с предметом и новым балансом
- **Обработка ошибок:**
  - `NotFoundError` если кейс/пользователь не найден
  - `ValidationError` если недостаточно средств
- **Порядок операций:**
  1. Валидация (пользователь, кейс, баланс)
  2. Weighted random выбор предмета
  3. Транзакция БД (списание, добавление предмета, запись открытия)
  4. WebSocket emit (live-feed)
  5. Обновление статистики пользователя (favorite case, best drop)
  6. Return результата
- **Примечание:**
  - WebSocket emit обёрнут в try-catch и не прерывает функционал
  - Обновление статистики обёрнуто в try-catch и не прерывает функционал

#### **`getRecentOpenings(limit)`**

- **Описание:** Получение последних открытий для live-ленты
- **Тип:** `(limit: number = 20) => Promise<ILiveFeedEvent[]>`
- **Логика:**
  - Загружает последние открытия кейсов
  - Включает данные пользователя, кейса и предмета
  - Форматирует в ILiveFeedEvent формат
  - Сортирует по дате открытия (новые первыми)
- **Использование:** Для public live-feed endpoint

### **⚠️ Important**

#### **Live Feed Integration:**

- ✅ **Real-time уведомления** - WebSocket события при каждом открытии
- ✅ **Room-based broadcast** - только в 'live-feed' комнату
- ✅ **Формат ILiveFeedEvent** - консистентный для frontend
- ✅ **Error handling** - emit не прерывает основную логику

#### **Weighted Random Algorithm:**

- **Math.random()** базовый генератор
- **Накопительные интервалы** для правильного распределения
- **Decimal → Number** конвертация для chancePercent

#### **Transaction Safety:**

- **Prisma $transaction** для атомарности
- **Баланс + инвентарь + история** в одной транзакции
- **Консистентное состояние** при сбоях

---

## 🎨 CS:GO API Service (`csApi.service.ts`)

Сервис для интеграции с GitHub API репозитория ByMykel/CSGO-API для получения данных о CS:GO скинах с локальным кэшированием.

### **Основные возможности**

- Получение полных данных о CS:GO скинах из JSON файлов GitHub
- Локальное кэширование данных в JSON файл
- Атомарная запись данных для безопасности
- Автоматическое создание директории для кэша
- Детальное логирование всех операций

### **Источник данных**

**API Endpoint:**

```
https://raw.githubusercontent.com/ByMykel/CSGO-API/main/public/api/ru/skins_not_grouped.json
```

**Характеристики:**

- Формат: JSON массив объектов
- Количество скинов: ~15,000+
- Язык: Русский (ru)
- Обновление: По требованию

### **Методы сервиса**

#### **`syncSkinsCache()`** - Синхронизация кэша

- **Описание:** Получает данные из API и обновляет локальный кэш
- **Тип:** `() => Promise<SyncResult>`
- **Логика:**
  1. Логирует начало синхронизации
  2. Вызывает `fetchSkinsFromAPI()` с timeout 30 сек
  3. Создает директорию `data/` если не существует
  4. Атомарно записывает данные через временный файл
  5. Логирует результат с статистикой
- **Возвращает:**
  ```typescript
  {
    lastSync: string; // ISO 8601 timestamp
    totalSkins: number; // Количество скинов
    duration: number; // Время выполнения в мс
  }
  ```
- **Обработка ошибок:**
  - HTTP timeout → `API request timeout after 30000ms`
  - HTTP ошибка → `API request failed with status XXX`
  - Ошибка записи → `Failed to write cache file`
- **Использование:** Вызывается из cron job для обновления данных

#### **`getSkinsFromCache()`** - Чтение из кэша

- **Описание:** Получает скины из локального JSON кэша
- **Тип:** `() => Promise<CSApiSkin[]>`
- **Логика:**
  - Читает файл `data/skins-cache.json`
  - Парсит JSON и возвращает массив скинов
  - Если файл не существует → возвращает `[]`
  - Если данные невалидны → возвращает `[]` + warning лог
- **Возвращает:** Массив скинов или пустой массив
- **Обработка ошибок:** Все ошибки логируются, метод не выбрасывает исключения
- **Использование:** Быстрое получение данных без запроса к API

#### **`getCacheInfo()`** - Информация о кэше

- **Описание:** Получает метаданные о состоянии кэша
- **Тип:** `() => Promise<CacheInfo>`
- **Логика:**
  - Читает метаданные из кэша
  - Возвращает информацию о последней синхронизации
- **Возвращает:**
  ```typescript
  {
    lastSync: string | null; // Время последней синхронизации
    totalSkins: number; // Количество скинов
    cacheExists: boolean; // Существует ли кэш
  }
  ```
- **Использование:** Проверка актуальности кэша, отображение статистики

### **Структура данных**

#### **CSApiSkin Interface**

```typescript
interface CSApiSkin {
  id: string;
  name: string; // Локализованное название
  description: string; // Описание скина
  weapon: {
    id: string;
    name: string; // Название оружия
  };
  category: {
    id: string;
    name: string; // Категория (Винтовки, Пистолеты и т.д.)
  };
  pattern: {
    id: string;
    name: string; // Название паттерна
  };
  min_float: number; // Минимальное значение износа
  max_float: number; // Максимальное значение износа
  rarity: {
    id: string;
    name: string; // Редкость (запрещённого, тайного и т.д.)
    color: string; // HEX цвет редкости
  };
  stattrak: boolean; // Поддержка StatTrak™
  souvenir: boolean; // Сувенирная версия
  paint_index: string; // Индекс окраски
  wears: Array<{
    id: string;
    name: string; // Состояния (Factory New, Minimal Wear и т.д.)
  }>;
  market_hash_name: string | null; // Steam Market hash name
  image: string; // URL изображения
}
```

#### **SkinsCacheData Interface**

```typescript
interface SkinsCacheData {
  lastSync: string; // ISO 8601 timestamp последней синхронизации
  totalSkins: number; // Общее количество скинов
  skins: CSApiSkin[]; // Массив всех скинов
}
```

### **Путь к кэшу**

- **Директория:** `server/data/`
- **Файл:** `skins-cache.json`
- **Размер:** ~35 MB (JSON с форматированием)
- **Gitignore:** Кэш не коммитится в репозиторий

### **Технические детали**

#### **Атомарная запись**

Для предотвращения повреждения кэша при сбое используется паттерн atomic write:

1. Данные записываются во временный файл `.tmp`
2. Временный файл переименовывается в финальный (атомарная операция ОС)
3. Никогда не будет частично записанного кэша

#### **Обработка ошибок HTTP**

- **Timeout (30 сек)** → выбрасывает ошибку с кодом `ECONNABORTED`
- **HTTP 4xx/5xx** → выбрасывает ошибку со статусом ответа
- **Network error** → выбрасывает общую ошибку подключения
- Все ошибки детально логируются через Winston

#### **Логирование**

**События (info level):**

- Начало синхронизации с URL источника
- Успешное получение данных (+ количество)
- Успешное сохранение кэша (+ путь)
- Завершение синхронизации (+ статистика)

**Ошибки (error level):**

- Ошибки HTTP запросов
- Ошибки записи файлов
- Валидационные ошибки данных

**Предупреждения (warn level):**

- Невалидный формат кэша
- Отсутствие обязательных полей

### **Использование**

#### **Пример синхронизации**

```typescript
import { csApiService } from './services/csApi.service.js';

// Синхронизация данных
const result = await csApiService.syncSkinsCache();
console.log(`Синхронизировано ${result.totalSkins} скинов за ${result.duration}мс`);
```

#### **Пример получения данных**

```typescript
// Получить все скины из кэша
const skins = await csApiService.getSkinsFromCache();
console.log(`Найдено ${skins.length} скинов в кэше`);

// Фильтрация по оружию
const ak47Skins = skins.filter((skin) => skin.weapon.id === 'ak47');

// Поиск по названию
const dragonsLore = skins.find(
  (skin) => skin.market_hash_name === 'AWP | Dragon Lore (Factory New)'
);
```

#### **Пример проверки кэша**

```typescript
// Проверить состояние кэша
const info = await csApiService.getCacheInfo();

if (!info.cacheExists) {
  console.log('Кэш не найден, требуется синхронизация');
  await csApiService.syncSkinsCache();
} else {
  const lastSyncDate = new Date(info.lastSync);
  const hoursSinceSync = (Date.now() - lastSyncDate.getTime()) / (1000 * 60 * 60);

  if (hoursSinceSync > 24) {
    console.log('Кэш устарел, обновляем...');
    await csApiService.syncSkinsCache();
  }
}
```

### **⚠️ Important**

#### **Не использует Prisma:**

- ✅ Работает с JSON файлами напрямую
- ✅ Не создает таблицы в БД
- ✅ Полностью автономный сервис
- 🚫 Не требует миграций БД

#### **Интеграция с cron:**

- Готов к использованию в cron job
- Рекомендуется синхронизация 1 раз в сутки
- Можно запускать вручную через админ панель

#### **Производительность:**

- Первая синхронизация: ~4-5 секунд
- Чтение из кэша: <100 мс
- Размер кэша: ~35 MB
- Количество скинов: ~15,000

---

## 🔍 Admin Skins Service (`admin/adminSkins.service.ts`)

Сервис для фильтрации и работы со скинами из кэша в памяти (только для администраторов).

### **Особенности сервиса**

- **Работает с кэшем в памяти** - skinsCache.util.ts с индексами
- **Мгновенные запросы** - нет обращений к БД при фильтрации
- **Высокопроизводительный поиск** - использование индексов для rarity/weapon
- **Интеграция с CS API** - возможность ручной синхронизации

### **Функции работы со скинами**

#### **`getFilteredSkins(filters)`** - Фильтрация скинов с пагинацией

- **Тип:** `(filters: SkinFilters) => FilteredSkinsResult`
- **Логика:**
  - Использует skinsCache.search() для базовой фильтрации (если есть rarity/weapon/query)
  - Применяет дополнительные фильтры (categoryId, patternId, wearId, stattrak, souvenir)
  - Применяет сортировку по name/rarity/weapon/category
  - Применяет пагинацию
  - Возвращает результат с пагинацией
- **Параметры фильтрации:**
  - `search?: string` - поиск по name или market_hash_name (регистронезависимый)
  - `weaponId?: string` - точное совпадение weapon.id
  - `categoryId?: string` - точное совпадение category.id
  - `rarityId?: string` - точное совпадение rarity.id
  - `patternId?: string` - точное совпадение pattern.id
  - `wearId?: string` - точное совпадение wear.id
  - `stattrak?: boolean` - фильтр по StatTrak
  - `souvenir?: boolean` - фильтр по Souvenir
  - `page?: number` - номер страницы (default: 1)
  - `limit?: number` - лимит на странице (default: 50, max: 500)
  - `sortBy?: 'name' | 'rarity' | 'weapon' | 'category'` - поле сортировки (default: 'name')
  - `sortOrder?: 'asc' | 'desc'` - направление сортировки (default: 'asc')
- **Возвращает:**
  ```typescript
  {
    skins: CachedSkin[];          // Отфильтрованные скины с пагинацией
    pagination: {
      total: number;              // Общее количество после фильтров
      page: number;               // Текущая страница
      limit: number;              // Лимит на странице
      totalPages: number;         // Всего страниц
    };
  }
  ```
- **Использование:** Основной метод для фильтрации и поисков в админке

#### **`getSkinById(id)`** - Поиск скина по ID

- **Тип:** `(id: string) => CachedSkin | null`
- **Логика:**
  - Использует skinsCache.findById(id)
  - Возвращает скин или null
- **Особенности:** Синхронный метод, не использует Promise
- **Использование:** Детальная информация о скине по уникальному ID

#### **`getAvailableFilters()`** - Доступные фильтры

- **Тип:** `() => AvailableFilters`
- **Логика:**
  - Использует skinsCache.getAllWeapons() и getAllRarities() (с индексами)
  - Получает все скины и собирает уникальные категории, паттерны, износы
  - Сортирует все списки по name
- **Возвращает:**
  ```typescript
  {
    weapons: Array<{ id: string; name: string }>; // Уникальные оружия
    categories: Array<{ id: string; name: string }>; // Уникальные категории
    rarities: Array<{
      // Уникальные редкости
      id: string;
      name: string;
      color: string;
    }>;
    patterns: Array<{ id: string; name: string }>; // Уникальные паттерны
    wears: Array<{ id: string; name: string }>; // Уникальные износы
  }
  ```
- **Использование:** Построение UI фильтров в админке

#### **`getSkinsStats()`** - Статистика по скинам

- **Тип:** `() => SkinsStats`
- **Логика:**
  - Использует skinsCache.getStats() для базовой статистики
  - Получает все скины через приватный метод getAllSkins()
  - Считает уникальные категории, паттерны, износы
  - Считает количество stattrak/souvenir
- **Возвращает:**
  ```typescript
  {
    totalSkins: number; // Общее количество скинов
    totalWeapons: number; // Уникальных оружий
    totalCategories: number; // Уникальных категорий
    totalRarities: number; // Уникальных редкостей
    totalPatterns: number; // Уникальных паттернов
    totalWears: number; // Уникальных износов
    stattrakCount: number; // Скинов с StatTrak
    souvenirCount: number; // Сувенирных скинов
  }
  ```
- **Использование:** Информационная панель статистики в админке

#### **`syncSkinsFromApi()`** - Ручная синхронизация

- **Тип:** `() => Promise<SyncResult>`
- **Логика:**
  - Вызывает csApiService.syncSkinsCache()
  - После успешной синхронизации вызывает skinsCache.reload()
  - Возвращает результат синхронизации
- **Возвращает:** `SyncResult` из csApiService
- **Особенности:** Единственный async метод, который выполняется долго (~4-5 сек)
- **Использование:** Ручное обновление скинов через админку при изменениях в данных

### **Приватные методы**

#### **`getAllSkins()`** - Вспомогательный метод

- **Тип:** `() => CachedSkin[]`
- **Логика:**
  - Вызывает skinsCache.search({}) без параметров
  - Возвращает все скины из кэша
- **Примечание:** skinsCache не предоставляет публичный метод получения всех скинов

### **Типы (экспортируемые)**

```typescript
// Параметры фильтрации
export interface SkinFilters {
  search?: string;
  weaponId?: string;
  categoryId?: string;
  rarityId?: string;
  patternId?: string;
  wearId?: string;
  stattrak?: boolean;
  souvenir?: boolean;
  page?: number;
  limit?: number;
  sortBy?: 'name' | 'rarity' | 'weapon' | 'category';
  sortOrder?: 'asc' | 'desc';
}

// Результат фильтрации
export interface FilteredSkinsResult {
  skins: CachedSkin[];
  pagination: {
    total: number;
    page: number;
    limit: number;
    totalPages: number;
  };
}

// Доступные фильтры
export interface AvailableFilters {
  weapons: Array<{ id: string; name: string }>;
  categories: Array<{ id: string; name: string }>;
  rarities: Array<{ id: string; name: string; color: string }>;
  patterns: Array<{ id: string; name: string }>;
  wears: Array<{ id: string; name: string }>;
}

// Статистика скинов
export interface SkinsStats {
  totalSkins: number;
  totalWeapons: number;
  totalCategories: number;
  totalRarities: number;
  totalPatterns: number;
  totalWears: number;
  stattrakCount: number;
  souvenirCount: number;
}
```

### **Интеграции**

#### **Зависимости:**

- `skinsCache` из `../../utils/skinsCache.util.js` - основной кэш данных
- `csApiService` из `../csApi.service.js` - только для syncSkinsFromApi()
- `logger` из `../../middleware/logger.middleware.js` - детальное логирование

#### **CachedSkin тип:**

Импортируется из skinsCache.util.ts:

```typescript
interface CachedSkin {
  id: string;
  skin_id: string;
  name: string;
  description: string;
  weapon: { id: string; name: string };
  category: { id: string; name: string };
  pattern: { id: string; name: string };
  min_float: number;
  max_float: number;
  wear: { id: string; name: string };
  stattrak: boolean;
  souvenir: boolean;
  paint_index: string;
  rarity: { id: string; name: string; color: string };
  market_hash_name: string;
  team: { id: string; name: string };
  style: { id: number; name: string; url: string | null };
  legacy_model: boolean;
  image: string;
  original: { name: string; image_inventory: string };
}
```

### **⚠️ Особенности реализации**

#### **Производительность:**

- ✅ **Мгновенные запросы** - работа полностью в памяти (~1-5 мс)
- ✅ **Оптимизированные индексы** - прямая доступ к rarity/weapon через Map
- ✅ **Нет обращений к БД** - используется только skinsCache
- ✅ **Эффективная сортировка** - localeCompare для строк

#### **Обработка ошибок:**

- ✅ **Пустой кэш** - возвращает пустые результаты без ошибок
- ✅ **Валидация параметров** - стандартные значения по умолчанию
- ✅ **Логирование через Winston** - debu g/info/error уровни
- ✅ **Граничные случаи** - некорректные данные не приводят к краху

#### **Безопасность:**

- ✅ **Только чтение кэша** - модификация только через syncSkinsFromApi()
- ✅ **Только для администраторов** - должен использоваться с requireAdmin middleware
- ✅ **Защита от инъекций** - нет прямых SQL запросов к БД

#### **Rate limiting:**

- ✅ **Агрессивные лимиты** на POST /sync (5 req / 15 мин)
- ✅ **Тяжёлая операция** - синхронизация занимает ~4-5 секунд
- ✅ **Защита от DoS** - не может быть использована для атаки

---

## 🚀 Future Services (Планируемые)

### **Payment Service** (`payment.service.ts`)

- Интеграция с платёжными системами
- Пополнение баланса
- Обработка транзакций

### **Admin Service** (`admin.service.ts`)

- CRUD операций для кейсов
- Управление предметами
- Аналитика и статистика

---

## 📋 Best Practices

### **Функции сервисов:**

1. **Асинхронные функции** с Promise
2. **Строгая типизация** входов/выходов
3. **Использование utils** для ошибок
4. **Простые Prisma запросы**
5. **Нет HTTP зависимостей**

### **Организация кода:**

- **Один файл = один домен** (cases, users, payments)
- **Pure functions** без побочных эффектов
- **Documented interfaces** в TYPES_REFERENCE.md
- **Consistent naming** (get*, find*, create\*)

---

_Последнее обновление: 17.10.2025_
