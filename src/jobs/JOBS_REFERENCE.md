# 🕐 Задачи - Справочник

## 📖 Описание

Справочник фоновых задач (cron jobs) проекта с описанием запланированных операций в папке `src/jobs/`.

---

## 🔄 Sync Items Job (`syncItems.job.ts`)

Автоматическая синхронизация скинов CS:GO из GitHub API (ByMykel/CSGO-API) с локальным кэшированием.

### **Назначение**

- Периодическое обновление кэша скинов из CSGO-API
- Получение актуальных данных о скинах (названия, редкость, цены)
- Поддержание актуальности данных для системы кейсов
- Локальное кэширование для быстрого доступа (~15,000 скинов)

### **Расписание**

- **Время:** Ежедневно в 3:00 ночи
- **Cron выражение:** `0 3 * * *`
- **Часовой пояс:** Серверное время
- **Автоматический старт:** При запуске сервера
- **Timeout:** 30 секунд на API запрос

### **Исполнение**

```typescript
export const startItemsSyncJob = () => {
  cron.schedule('0 3 * * *', async () => {
    try {
      logger.info('🔄 Starting scheduled CSGO skins synchronization');

      // Проверка текущего состояния кэша
      const cacheInfoBefore = await csApiService.getCacheInfo();

      // Выполнение синхронизации
      const syncResult = await csApiService.syncSkinsCache();

      logger.info('✅ Scheduled synchronization completed', {
        totalSkins: syncResult.totalSkins,
        duration: syncResult.duration,
        skinsAdded: syncResult.totalSkins - cacheInfoBefore.totalSkins,
      });
    } catch (error) {
      logger.error('❌ Synchronization failed', { error });
    }
  });

  logger.info('✅ Cron job started (Every day at 3:00 AM)');
};
```

### **Процесс работы**

1. **Запуск по расписанию** - Cron job активируется в 3:00
2. **Проверка кэша** - `csApiService.getCacheInfo()` получает текущее состояние
3. **API запрос** - Получение данных из `https://raw.githubusercontent.com/ByMykel/CSGO-API/main/public/api/ru/skins_not_grouped.json`
4. **Локальное кэширование** - Атомарная запись в `data/skins-cache.json`
5. **Валидация** - Проверка структуры данных после синхронизации
6. **Детальное логирование** - Запись всех этапов и результатов

### **Источник данных**

- **API:** GitHub Raw Content (ByMykel/CSGO-API)
- **URL:** `https://raw.githubusercontent.com/ByMykel/CSGO-API/main/public/api/ru/skins_not_grouped.json`
- **Язык:** Русский (ru)
- **Количество скинов:** ~15,000+
- **Размер:** ~35 MB JSON
- **Timeout:** 30 секунд

### **Логирование**

**Успешная синхронизация:**

```
19:47:21 info: 🔄 Starting scheduled CSGO skins synchronization
19:47:21 info: Cache state before sync { cacheExists: true, totalSkins: 15339 }
19:47:25 info: Успешно извлеченные скины из API { totalSkins: 15339 }
19:47:25 info: ✅ Scheduled synchronization completed { totalSkins: 15339, duration: "4272ms" }
19:47:25 info: Cache state after sync { cacheExists: true, totalSkins: 15339 }
```

**Ошибка синхронизации:**

```
19:47:21 info: 🔄 Starting scheduled CSGO skins synchronization
19:47:30 error: ❌ Scheduled synchronization failed { error: "API request timeout after 30000ms" }
```

### **Результат синхронизации**

```typescript
interface SyncResult {
  lastSync: string; // ISO 8601 timestamp
  totalSkins: number; // Количество скинов в кэше
  duration: number; // Время выполнения в мс
}
```

**Пример:**

```typescript
{
  lastSync: "2025-10-23T16:47:25.414Z",
  totalSkins: 15339,
  duration: 4272
}
```

### **Ручной запуск**

#### CLI команда:

```bash
# Синхронизация
npm run sync-skins

# Информация о кэше
npm run sync-skins:info

# Прямой вызов
npx tsx sync-skins.ts
```

#### Программный вызов:

```typescript
import { manualSyncItems } from './jobs/syncItems.job.js';

// Ручной запуск синхронизации
await manualSyncItems();
```

### **Обработка ошибок**

- **HTTP Timeout (>30s)** - Ошибка логируется, следующая попытка через 24 часа
- **API недоступен (4xx/5xx)** - Детальное логирование статуса и причины
- **Ошибка записи файла** - Использование временного файла предотвращает повреждение кэша
- **Сетевые проблемы** - Детальное логирование с информацией о соединении
- **Невалидные данные** - Проверка структуры, откат к предыдущему кэшу

### **Производительность**

- **Первая синхронизация:** 4-5 секунд
- **Повторная синхронизация:** 4-5 секунд (полная перезапись)
- **Чтение из кэша:** <100 мс
- **Размер кэша:** ~35 MB
- **Атомарность:** Временный файл + rename (безопасная запись)

### **Кэш**

- **Путь:** `server/data/skins-cache.json`
- **Формат:** JSON с форматированием
- **Структура:**
  ```json
  {
    "lastSync": "2025-10-23T16:47:25.414Z",
    "totalSkins": 15339,
    "skins": [
      /* массив скинов */
    ]
  }
  ```
- **Gitignore:** Кэш не коммитится в репозиторий
- **Backup:** Рекомендуется создавать копии перед обновлением production

---

## 🏗️ Архитектура задач

### **Структура файлов**

```
src/jobs/
├── syncItems.job.ts     # Синхронизация скинов
└── JOBS_REFERENCE.md    # Справочник задач
```

### **Интеграция с сервером**

```typescript
// server.ts
import { startItemsSyncJob } from './jobs/syncItems.job.js';

const startServer = async () => {
  await testDatabaseConnection();
  startItemsSyncJob(); // Запуск всех cron jobs
  // ... старт сервера
};
```

### **Зависимости**

- **node-cron** - Библиотека для планирования задач
- **Services** - Бизнес-логика задач
- **Logger** - Логирование операций
- **Prisma** - Работа с базой данных

---

## 🚀 Future Jobs (Планируемые)

### **Database Backup Job**

- **Назначение:** Автоматическое резервное копирование БД
- **Расписание:** Ежедневно в 4:00
- **Функционал:** Дамп, архивирование, удаление старых копий

### **Cache Cleanup Job**

- **Назначение:** Очистка устаревшего кэша
- **Расписание:** Ежечасно
- **Функционал:** Удаление просроченных записей, освобождение памяти

### **Analytics Job**

- **Назначение:** Сбор и агрегация статистики
- **Расписание:** Ежедневно в 5:00
- **Функционал:** Подсчет метрик, сохранение отчетов

### **Email Notifications Job**

- **Назначение:** Отправка email уведомлений
- **Расписание:** Каждые 15 минут
- **Функционал:** Обработка очереди, batch отправка

---

## 📋 Best Practices

### **Разработка задач**

1. **Изоляция** - Каждая задача в отдельном файле
2. **Логирование** - Детальное логирование всех этапов
3. **Обработка ошибок** - Graceful error handling
4. **Тестирование** - Unit тесты для логики задач
5. **Конфигурация** - Возможность отключения через config

### **Производительность**

- **Non-blocking** - Задачи не блокируют основной поток
- **Batch операции** - Группировка операций с БД
- **Timeout** - Ограничение времени выполнения
- **Resource limits** - Контроль использования памяти

### **Безопасность**

- **Permissions** - Минимальные права для задач
- **Validation** - Проверка данных перед обработкой
- **Audit** - Логирование всех важных операций

### **Мониторинг**

- **Health checks** - Проверка состояния задач
- **Metrics** - Сбор метрик выполнения
- **Alerts** - Уведомления о критических ошибках
- **Logs** - Централизованное хранение логов

---

## 📝 Использование

### **Создание новой задачи**

```typescript
// jobs/example.job.ts
import cron from 'node-cron';
import { logger } from '../middleware/logger.middleware.js';

export const startExampleJob = () => {
  cron.schedule('*/30 * * * *', async () => {
    try {
      logger.info('Запуск примера задачи');
      // Логика задачи
      logger.info('Пример задачи завершен');
    } catch (error) {
      logger.error('Ошибка в примере задачи', { error });
    }
  });

  logger.info('Example job started');
};
```

### **Интеграция в сервер**

```typescript
// server.ts
import { startExampleJob } from './jobs/example.job.js';

// После подключения к БД
startExampleJob();
```

### **Отключение задачи**

```typescript
// Через environment variable
if (config.enableExampleJob) {
  startExampleJob();
}
```

---

## 🔧 Конфигурация

### **Environment Variables**

```typescript
// env.config.ts
export const config = {
  jobs: {
    syncItems: {
      enabled: boolean,
      schedule: string, // '0 3 * * *'
      timeout: number, // 300000
    },
  },
};
```

### **Динамическая конфигурация**

```typescript
// jobs/config.job.ts
export const getJobConfig = (jobName: string) => {
  return {
    enabled: config.jobs[jobName].enabled,
    schedule: config.jobs[jobName].schedule,
    timeout: config.jobs[jobName].timeout,
  };
};
```

---

_Последнее обновление: 17.10.2025_
