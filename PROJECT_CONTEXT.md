# Контекст проекта для AI

## 📋 Обзор проекта

Backend сервер для платформы открытия кейсов CS:GO на Node.js + Express + TypeScript + Prisma.

---

## 📚 Справочники

### Утилиты

- **Путь**: `server/src/utils/UTILS_REFERENCE.md`
- **Содержание**: Полный справочник утилит проекта (error handling, response, JWT, helpers)

### Типы

- **Путь**: `server/src/types/TYPES_REFERENCE.md`
- **Содержание**: Справочник типов и констант проекта (user types, constants, express types)

### Middleware

- **Путь**: `server/src/middleware/MIDDLEWARE_REFERENCE.md`
- **Содержание**: Справочник middleware проекта (security, rate limiting, logging, error handling)

### Конфигурация

- **Путь**: `server/src/config/CONFIG_REFERENCE.md`
- **Содержание**: Справочник конфигурации проекта (environment variables, database, passport)

### Контроллеры

- **Путь**: `server/src/controllers/CONTROLLERS_REFERENCE.md`
- **Содержание**: Справочник контроллеров проекта (authentication, OAuth, user management)

### Роуты

- **Путь**: `server/src/routes/ROUTES_REFERENCE.md`
- **Содержание**: Справочник роутов проекта (API endpoints, versioning, authentication)

### Сервисы

- **Путь**: `server/src/services/SERVICES_REFERENCE.md`
- **Содержание**: Справочник сервисов проекта (business logic, database operations)

### Задачи (Jobs)

- **Путь**: `server/src/jobs/JOBS_REFERENCE.md`
- **Содержание**: Справочник фоновых задач проекта (cron jobs, scheduled tasks)

---

## 🏗️ Архитектура проекта

### Структура директорий

```
## 📂 logs — структура и описание файлов

logs/
│   ├── 📄 combined.log
│   ├── 📄 error.log
│   ├── 📄 exceptions.log
│   └── 📄 rejections.log

## 📂 prisma — структура и описание файлов

prisma/
│   ├── 📁 migrations
│   │   ├── 📁 20251015170232_init
│   │   │   └── 📄 migration.sql
│   │   └── 📄 migration_lock.toml
│   └── 📄 schema.prisma

## 📂 src — структура и описание файлов

src/
├── config/                 # Конфигурация приложения + справочник
│   ├── database.ts         # Prisma-клиент и подключение к БД (экспорт `prisma`/`db`, транзакции)
│   ├── env.config.ts       # Загрузка env + Zod-валидация, экспорт типизированной конфигурации
│   ├── passport.config.ts  # Passport стратегии (Steam, VK), serialize/deserialize, колбэки
│   └── CONFIG_REFERENCE.md # 📖 Документация переменных окружения и их значений
├── controllers/            # Контроллеры обработки HTTP-запросов + справочник
│   ├── auth.controller.ts  # OAuth (login, callback, logout), получение профиля пользователя
│   ├── case.controller.ts  # Публичные эндпоинты по кейсам (чтение: list, get by id)
│   ├── caseOpening.controller.ts # Open-case endpoints, SSE/WebSocket live-лента событий открытия
│   ├── user.controller.ts  # Профиль пользователя, инвентарь, история операций
│   └── CONTROLLERS_REFERENCE.md # 📖 Описание контроллеров и их контрактов
├── middleware/             # Express middleware + справочник
│   ├── auth.middleware.ts         # JWT/Passport проверка доступа, guard для роутов
│   ├── errorHandler.middleware.ts # Централизованный обработчик ошибок (формат ответа, код)
│   ├── logger.middleware.ts       # Winston + Morgan — логирование запросов и ошибок
│   ├── rateLimiter.middleware.ts  # Ограничение частоты запросов (IP, user)
│   ├── security.middleware.ts     # Helmet, CORS, заголовки безопасности
│   ├── MIDDLEWARE_REFERENCE.md    # 📖 Описание middleware и их настроек
│   └── index.ts                   # Экспорт и объединение всех middleware
├── utils/                  # Утилиты и хелперы + справочник
│   ├── errors.util.ts      # Кастомные ошибки (AppError, ValidationError и т.д.)
│   ├── helpers.util.ts     # Общие хелперы: slugify, formatPrice, clamp, sleep и т.п.
│   ├── index.ts            # Централизованный экспорт утилит
│   ├── jwt.util.ts         # Генерация/верификация JWT, refresh token helpers
│   ├── response.util.ts    # Утилиты для стандартного API-ответа (success/error wrappers)
│   └── UTILS_REFERENCE.md  # 📖 Документация утилит и рекомендации по использованию
├── types/                  # TypeScript типы + справочник
│   ├── case.types.ts       # Типы: Case, CaseItem, CaseMetadata
│   ├── caseOpening.types.ts # Типы для процесса открытия (request/response, events)
│   ├── constants.ts        # Константы как `as const`: роли, статусы, редкости
│   ├── express.d.ts        # Расширение типов Express (Request.user и т.п.)
│   ├── index.ts            # Баррель-экспорт всех типов
│   ├── item.types.ts       # Типы предметов: Item, Skin, float/rarity и метаданные
│   ├── payment.types.ts    # Типы платежей и YooKassa (create, webhook payloads)
│   ├── TYPES_REFERENCE.md  # 📖 Описание типов и контрактов API
│   └── user.types.ts       # User, Profile, Inventory, Wallet типы
├── jobs/                   # Фоновые задачи (cron jobs) + справочник
│   ├── syncItems.job.ts    # Cron: синхронизация с market.csgo.com, нормализация данных и кэш
│   └── JOBS_REFERENCE.md   # 📖 Описание задач, расписание и требования
├── routes/                 # Express роуты + справочник
│   ├── auth.routes.ts      # Эндпоинты OAuth (login, callback, logout)
│   ├── case.routes.ts      # Публичные эндпоинты по кейсам (list, detail)
│   ├── caseOpening.routes.ts # Роуты для открытия кейсов и подписки на live-ленты
│   ├── index.ts            # Централизованный роутер (mount всех роутеров, префиксы)
│   ├── ROUTES_REFERENCE.md # 📖 Справочник роутов и примеры запросов
│   └── user.routes.ts      # Эндпоинты пользователя (inventory, history, profile)
├── services/               # Бизнес-логика + справочник
│   ├── case.service.ts     # Доступ к данным кейсов (DB чтение, кеширование)
│   ├── caseOpening.service.ts # Алгоритм открытия кейсов, расчёт шансов, обновление баланса
│   ├── marketCs.service.ts  # Интеграция с market.csgo.com: запросы, трансформация, retry
│   ├── payment.service.ts  # YooKassa: создание платежей, подтверждение, обработка вебхуков
│   ├── SERVICES_REFERENCE.md # 📖 Описание сервисов и их контрактов
│   └── user.service.ts     # Операции с пользователем: профиль, инвентарь, транзакции
├── app.ts                  # Конфигурация Express-приложения (middleware, роуты, healthchecks)
└── server.ts               # Запуск сервера: подключение к БД, инициализация задач, graceful shutdown
.env
.env.example
.gitignore
.prettierrc
eslint.config.js
nodemon.json
package.json
PROJECT_CONTEXT.md
README.md
tsconfig.json
```

---

## 🚀 Ключевые компоненты и технологии

### Backend Framework

- **Express.js** - Веб-фреймворк для Node.js с middleware архитектурой
- **TypeScript** - Типизированный JavaScript для безопасности типов
- **Prisma ORM** - Type-safe работа с PostgreSQL базой данных

### Аутентификация и авторизация

- **Passport.js** - Middleware для OAuth аутентификации
- **passport-steam** - Steam OpenID интеграция
- **passport-vkontakte** - VK OAuth 2.0 интеграция
- **JWT** - Stateless токены для сессий пользователей

### Платежная система

- **YooKassa API v3** - Прием платежей от пользователей
- **UUID** - Генерация idempotence ключей для безопасности
- **Webhook обработка** - Асинхронное получение статусов платежей

### Внешние интеграции

- **market.csgo.com API** - Автоматическое обновление скинов
- **node-cron** - Планировщик фоновых задач
- **Axios** - HTTP клиент для внешних запросов

### Безопасность и мониторинг

- **Helmet** - Установка security HTTP headers
- **CORS** - Cross-Origin Resource Sharing
- **Rate Limiting** - Защита от DDoS и злоупотреблений
- **Winston + Morgan** - Централизованное логирование
- **Zod** - Валидация environment variables

### База данных

- **PostgreSQL** - Реляционная СУБД с ACID транзакциями
- **Database Transactions** - Атомарные операции для финансовых данных
- **Connection Pooling** - Оптимизация подключений к БД

---

## 🔧 Конфигурация и Environment

### Environment Variables (.env.example)

- **Базовые**: `NODE_ENV`, `PORT`
- **База данных**: `DATABASE_URL`
- **JWT**: `JWT_SECRET`, `JWT_EXPIRES_IN`
- **OAuth**: `STEAM_API_KEY`, `VK_APP_ID`, `VK_APP_SECRET`
- **Внешние сервисы**: `YOOKASSA_SHOP_ID`, `MARKET_CS_API_KEY`
- **Безопасность**: `CORS_ORIGIN`, `RATE_LIMIT_WINDOW_MS`

---

## 🔄 Основные бизнес-процессы

### 1. Аутентификация пользователей

- **Steam OAuth**: Пользователь → Steam → Backend → JWT токен → Frontend
- **VK OAuth**: Пользователь → VK → Backend → JWT токен → Frontend
- **Stateless**: JWT токены хранятся на клиенте, проверяются на backend

### 2. Управление кейсами

- **Чтение**: Публичный доступ к списку кейсов и содержимому
- **Открытие**: JWT → Проверка баланса → Алгоритм рандома → БД транзакция
- **Live-лента**: WebSocket трансляция открытий всем клиентам

### 3. Платежная система

- **Создание**: Запрос → Валидация → YooKassa API → Редирект
- **Обработка**: Webhook → Проверка → Зачисление → Статус
- **Безопасность**: Idempotence ключи, транзакции, metadata

### 4. Синхронизация данных

- **Автоматическая**: Cron job (3:00) → market.csgo.com → БД
- **Обновление**: Проверка существования → Создание/обновление цен
- **Логирование**: Детальное отслеживание процесса

### 5. Управление профилем

- **Инвентарь**: Получение предметов пользователя со статусом OWNED
- **История**: Открытия кейсов с детализацией
- **Баланс**: Финансовые операции через транзакции

---

## 📝 Архитектурные принципы

### Организация кода

- **Layered Architecture**: Controllers → Services → Database
- **TypeScript everywhere**: Строгая типизация всех компонентов
- **Documentation**: Комплексные справочники для каждого слоя

### Безопасность

- **Defense in Depth**: Множественные уровни защиты
- **Input Validation**: Zod схемы и middleware валидация
- **Rate Limiting**: Защита от злоупотреблений на всех уровнях

### Производительность

- **Database Optimization**: Транзакции, индексы, connection pooling
- **Caching Strategy**: Кэширование статических данных
- **Async Operations**: Неблокирующая обработка запросов

### Масштабируемость

- **Stateless Design**: JWT аутентификация без server state
- **Microservice Ready**: Четкое разделение ответственности
- **Horizontal Scaling**: Подготовка к load balancing

---

## 🚧 Разработка и обслуживание

### Запуск проекта

```bash
# Development
npm run dev          # Запуск с hot-reload (tsx)

# Production
npm run build         # TypeScript компиляция
npm start            # Запуск production версии
```

### Мониторинг

- **Logging**: Winston с уровнями (error, warn, info, debug)
- **Health Checks**: Эндпоинт `/health` для мониторинга
- **Error Tracking**: Централизованная обработка ошибок

### Тестирование и отладка

- **Type Safety**: Проверка типов на этапе компиляции
- **Zod Validation**: Runtime валидация данных
- **Environment Aware**: Разные настройки для dev/prod

---
